services:
  - type: web
    name: nifty-midcap-screener
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --workers 1 --threads 4 --timeout 300
    envVars:
      - key: SECRET_KEY
        generateValue: true
